name: Clean Cloud Infrastructure

permissions: write-all

on:
  workflow_dispatch:

jobs:
  terraform-destroy:
    name: Deleting Cloud Infrastructure
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./infrastructure

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS || github.event.inputs.azure_credentials }}

      - name: Read run ID
        id: get-run-id
        run: |
          RUN_ID_PATH="../assets/terraform_state/run_id.txt"
          if [ ! -f "$RUN_ID_PATH" ]; then
            echo "❌ Arquivo run_id.txt não encontrado em $RUN_ID_PATH!"
            echo "⚠️ Infraestrutura não criada para exclusão."
            echo "🛑 Encerrando o workflow."
            exit 1
          fi

          if [ ! -s "$RUN_ID_PATH" ]; then
            echo "❌ Arquivo run_id.txt está vazio em $RUN_ID_PATH!"
            echo "⚠️ Infraestrutura não criada para exclusão."
            echo "🛑 Encerrando o workflow."
            exit 1
          fi

          echo "RUN_ID=$(cat $RUN_ID_PATH)" >> "$GITHUB_ENV"

      - name: Download Terraform state from previous run
        uses: actions/download-artifact@v4
        with:
          name: terraform-state
          path: ./infrastructure
          run-id: ${{ env.RUN_ID }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.5.7

      - run: terraform init
      - run: terraform validate
      - run: terraform plan -var="subscription_id=$(az account show --query id -o tsv)"
      - run: terraform destroy -auto-approve -var="subscription_id=$(az account show --query id -o tsv)"
