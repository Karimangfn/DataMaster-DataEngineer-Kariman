apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingest-db.fullname" . }}
  labels:
    {{- include "ingest-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingest-db.selectorLabels" . | nindent 4 }}
