apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingest-files.fullname" . }}-test-connection"
  labels:
    {{- include "ingest-files.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: "{{ .Chart.Name }}"
      image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
      imagePullPolicy: {{ .Values.image.pullPolicy }}
  restartPolicy: Never
